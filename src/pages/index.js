import 'twin.macro';
import Hero from '../assets/Hero';
import Layout from '../components/Layout';

export default function Home() {
    return (
        <Layout>
            <Hero />
        </Layout>
    );
}
